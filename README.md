# VladX

**VladX** — современный язык программирования с синтаксисом на русском языке.

![Language](https://img.shields.io/badge/language-VladX-blue)
![License](https://img.shields.io/badge/license-MIT-green)

## О языке

VladX — это объектно-ориентированный язык с динамической типизацией, разработанный для русскоязычных разработчиков. Язык поддерживает как русские, так и английские ключевые слова, что делает его доступным для широкого круга разработчиков.

### Особенности

- 🇷🇺 **Русский синтаксис** — все ключевые слова доступны на русском и английском языках
- 🎯 **Динамическая типизация** — не нужно указывать типы переменных
- 🏗️ **Объектно-ориентированный** — классы, наследование, статические методы
- 📦 **Модульная система** — импорт и экспорт модулей
- ⚡ **Быстрая разработка** — простой и понятный синтаксис
- 🔧 **Расширяемость** — встроенные функции и возможность добавлять свои
- 🚀 **JIT-компиляция** — для улучшения производительности
- 🛡️ **Безопасность** — встроенные механизмы безопасности
- 🔍 **Отладка и профилирование** — интегрированные инструменты

## Быстрый старт

```vlad
класс Человек {
    конструктор(имя, возраст) {
        это.имя = имя
        это.возраст = возраст
    }

    приветствовать() {
        печать("Привет, меня зовут " + это.имя + ", мне " + это.возраст + " лет!")
    }
}

пусть человек = новый Человек("Иван", 25)
человек.приветствовать()
```

## Синтаксис и конструкции языка

### Переменные и константы

- **Переменные**: `пусть x = 10;` (эквивалент `let x = 10;`)
- **Константы**: `константа PI = 3.14;` (эквивалент `const PI = 3.14;`)
- **Деструктуризация**: `пусть [a, b] = [1, 2];`, `пусть {name, age} = user;`

### Типы данных

- **Числа**: `10`, `3.14`, научная нотация
- **Строки**: `"Hello"`, `'World'`, интерполяция шаблонов
- **Логические**: `истина` (true), `ложь` (false)
- **Массивы**: `[1, 2, 3]`, поддержка spread/rest
- **Объекты**: `{name: "John", age: 30}`
- **Null**: `ничто` (null)
- **Undefined**: `неопределено` (undefined)

### Операторы

- **Арифметические**: `+`, `-`, `*`, `/`, `%`, `**` (степень)
- **Сравнения**: `==`, `!=`, `<`, `>`, `<=`, `>=`
- **Логические**: `и` (&&), `или` (||), `не` (!)
- **Побитовые**: `&`, `|`, `^`, `~`, `<<`, `>>`, `>>>`
- **Присваивания**: `=`, `+=`, `-=`, `*=`, `/=`, `%=`, `**=`, `&=`, `|=`, `^=`, `<<=`, `>>=`, `>>>=`
- **Тернарный**: `condition ? trueValue : falseValue`

### Управление потоком

- **If/Else**:
  ```vlad
  если (condition) {
      // code
  } иначе {
      // code
  }
  ```

- **While**: `пока (condition) { // code }`
- **For**: `для (пусть i = 0; i < 10; i = i + 1) { // code }`
- **Break/Continue**: `прервать;`, `продолжить;`
- **Switch**:
  ```vlad
  переключатель (value) {
      случай 1: // code; прервать;
      случай 2: // code; прервать;
      по умолчанию: // code;
  }
  ```

### Функции

- **Объявление**:
  ```vlad
  функция add(a, b) {
      вернуть a + b;
  }
  ```

- **Асинхронные функции**:
  ```vlad
  асинхронная функция fetchData() {
      позволить result = ждать fetch('/api');
      вернуть result;
  }
  ```

- **Стрелочные функции**: `пусть add = (a, b) => a + b;`
- **Параметры по умолчанию**: `функция add(a, b = 0) { ... }`
- **Rest параметры**: `функция sum(...numbers) { ... }`
- **Генераторы**: `функция* range(start, end) { для (пусть i = start; i < end; i++) { yield i; } }`

### Классы и объекты

- **Объявление классов**:
  ```vlad
  класс Person {
      конструктор(name) {
          это.name = name;
      }

      приветствовать() {
          вернуть "Привет, " + это.name;
      }
  }
  ```

- **Наследование**: `класс Employee расширяет Person { ... }`
- **Статические методы**: `статический метод() { ... }`
- **Геттеры/сеттеры**: `получить name() { ... }`, `установить name(value) { ... }`

### Модули

- **Импорт**: `импорт {func} из "module";`
- **Экспорт**: `экспорт функция myFunc() { ... }`
- **Динамический импорт**: `пусть module = ждать импорт("module");`

### Обработка ошибок

```vlad
попробовать {
    // code that might throw
} поймать (error) {
    // handle error
} наконец {
    // cleanup
}
```

- **Выброс ошибки**: `выбросить new Ошибка("message");`

### Асинхронное программирование

- **Await**: `позволить result = ждать asyncFunction();`
- **Promises**: `обещание((resolve, reject) => { ... })`
- **Утилиты**: `параллельно()`, `последовательно()`, `гонка()`, `всеSettled()`, `любой()`, `повторить()`

## Встроенные функции

### Математические функции

- `максимум(a, b, ...)` - максимум чисел
- `минимум(a, b, ...)` - минимум чисел
- `случайный()` - случайное число (0-1)
- `случайноеЦелое(min, max)` - случайное целое
- `abs(n)`, `округлить(n)`, `пол(n)`, `потолок(n)` - abs, round, floor, ceil
- `корень(n)`, `степень(base, exp)`, `ln(n)`, `log10(n)` - sqrt, pow, ln, log10
- `sin(n)`, `cos(n)`, `tan(n)`, `asin(n)`, `acos(n)`, `atan(n)` - тригонометрия
- `пи()`, `e()` - константы π и e

### Строковые функции

- `длина(str)` - длина строки
- `нижнийРегистр(str)`, `верхнийРегистр(str)` - изменение регистра
- `обрезка(str)` - trim
- `заменить(str, old, new)` - замена
- `разделить(str, delimiter)` - разделение
- `соединить(arr, delimiter)` - соединение
- `подстрока(str, start, end)` - подстрока
- `кодСимвола(str, index)`, `символПоКоду(code)` - коды символов

### Функции массивов

- `создатьМассив(...items)` - создание массива
- `объединить(...arrays)` - объединение массивов
- `фильтр(arr, callback)` - фильтрация
- `отобразить(arr, callback)` - map
- `уменьшить(arr, callback, initial)` - reduce
- `найти(arr, callback)`, `найтиИндекс(arr, callback)` - find/findIndex
- `включает(arr, item)` - includes
- `сортировать(arr, comparator)` - сортировка
- `перевернуть(arr)` - reverse
- `копировать(arr)` - копирование
- `пустой(arr)` - проверка на пустоту
- `первый(arr)`, `последний(arr)` - первый/последний элемент
- `хвост(arr)`, `голова(arr)` - tail/head
- `добавить(arr, item)`, `вставить(arr, index, item)`, `удалить(arr, index)` - добавление/вставка/удаление
- `размер(arr)` - длина
- `каждый(arr, callback)`, `некоторые(arr, callback)` - every/some
- `срез(arr, start, end)` - slice
- `заполнить(arr, value, start, end)` - fill
- `развернуть(arr)`, `развернутьГлубоко(arr, depth)` - flat/flatMap

### Функции объектов

- `объект(obj)` - создание объекта
- `ключи(obj)`, `значения(obj)`, `пары(obj)` - keys/values/entries
- `слить(...objects)` - слияние объектов
- `из(obj, key)` - получение свойства
- `в(obj, key, value)` - установка свойства
- `удалитьСвойство(obj, key)` - удаление свойства
- `проверить(obj, key)` - проверка свойства

### Проверка и преобразование типов

- `тип(value)` - получение типа
- `этоЧисло(value)`, `этоСтрока(value)`, `этоМассив(value)`, `этоОбъект(value)`, `этоФункция(value)`, `этоЛогическое(value)` - проверки типов
- `этоПустой(value)` - проверка на пустоту
- `вСтроку(value)`, `вЧисло(value)`, `вМассив(value)` - преобразования типов

### Ввод/вывод и система

- `печать(...args)` - вывод в консоль
- `вывод(...args)` - вывод в stdout
- `ждать(ms)` - задержка
- `время()` - текущий timestamp
- `дата()` - текущая дата
- `ввести(prompt)` - ввод от пользователя
- `форматДаты(date, format)` - форматирование даты

### Работа с файлами

- `файлЧитать(path)` - чтение файла
- `файлЗаписать(path, content)` - запись файла
- `файлСуществует(path)` - проверка существования файла
- `файлПрочитатьПоток(path, options)` - чтение потоком
- `файлЗаписатьПоток(path, content, options)` - запись потоком
- `файлИнформация(path)` - информация о файле
- `директорияПрочитать(path, options)` - чтение директории
- `директорияСоздать(path, options)` - создание директории
- `файлУдалить(path)`, `директорияУдалить(path, options)` - удаление файла/директории
- `файлКопировать(src, dest)`, `файлПереместить(src, dest)` - копирование/перемещение
- `смотретьФайл(path, callback, options)` - наблюдение за файлом
- `найтиФайлы(path, pattern, options)` - поиск файлов
- `размерДиректории(path)` - размер директории
- `mimeТип(path)` - MIME-тип

### Сетевые операции

- `запрос(url, options)` - HTTP-запрос
- `httpGet(url, options)`, `httpPost(url, data, options)` - HTTP-методы
- `скачатьФайл(url, destPath)` - скачивание файла
- `загрузитьФайл(url, filePath, options)` - загрузка файла
- `проверитьURL(url)` - проверка URL
- `multipartForm()` - создание multipart-формы

### Криптография

- `хешMD5(str)`, `хешSHA256(str)` - хэширование
- `генUUID()` - генерация UUID

### Регулярные выражения

- `регex(pattern, flags)` - создание regex
- `сопоставить(str, regex)` - сопоставление
- `заменитьРВ(str, regex, replacement)` - замена с regex
- `разбитьРВ(str, regex)` - разделение с regex

### Функциональное программирование

- `частичное(fn, ...args)` - частичное применение
- `композиция(...fns)` - композиция функций
- `труба(val, ...fns)` - pipe-оператор
- `мемоизировать(fn)` - мемоизация
- `каррировать(fn)` - каррирование
- `инвертировать(fn)` - инверсия аргументов
- `одинРаз(fn)` - функция один раз
- `отладить(label)` - трассировка
- `можетБыть(value)` - Maybe-монда
- `илиИначе(maybe, defaultValue)` - getOrElse для Maybe

### Асинхронные утилиты

- `параллельно(tasks)` - параллельное выполнение
- `последовательно(tasks)` - последовательное выполнение
- `гонка(tasks)` - гонка (первый завершённый)
- `всеSettled(tasks)` - все settled
- `любой(tasks)` - любой (первый успешный)
- `повторить(fn, attempts)` - повтор
- `debounce(fn, delay)` - debounce
- `throttle(fn, limit)` - throttle
- `обещание(executor)` - создание promise
- `всеОбещания(promises)` - Promise.all
- `любоеОбещание(promises)` - Promise.any
- `всеОбещанияКакОбъект(obj)` - Promise.allSettled для объекта

### Структуры данных

- `Стек()` - Stack
- `Очередь()` - Queue
- `СвязныйСписок()` - LinkedList
- `ПриоритетнаяОчередь(comparator)` - PriorityQueue
- `Множество()` - Set
- `Карта()` - Map
- `Дерево()` - Trie
- `БинарноеДерево(compareFn)` - BinarySearchTree

### Генераторы

- `диапазон(start, end, step)` - генератор диапазона
- `генерировать(callback)` - пользовательский генератор

### Кэширование

- `кэшПолучить(key)` - получение из кэша
- `кэшУстановить(key, value)` - установка в кэш
- `кэшУдалить(key)` - удаление из кэша
- `кэшОчистить()` - очистка кэша
- `кэшСтатистика()` - статистика кэша

### Безопасность

- `проверитьПуть(path)` - валидация пути
- `проверитьURL(url)` - валидация URL
- `санитизировать(data)` - санитизация данных
- `экранироватьHTML(str)` - экранирование HTML

### Отладка и профилирование

- `точкаОстанова(filename, line)` - установка точки останова
- `удалитьТочкуОстанова(filename, line)` - удаление точки останова
- `пошаговыйРежим()` - step into
- `продолжить()` - continue
- `стекВызовов()` - стек вызовов
- `локальныеПеременные()` - локальные переменные
- `стартПрофилирования()`, `стопПрофилирования()`, `результатыПрофилирования()` - профилирование

### JSON

- `код(obj)` - stringify
- `разкод(str)` - parse

### Побитовые операции

- `битИ(a, b)`, `битИЛИ(a, b)`, `битИСКЛ(a, b)`, `битНЕ(a)` - побитовые AND, OR, XOR, NOT
- `сдвигВлево(a, n)`, `сдвигВправо(a, n)`, `беззнаковыйСдвиг(a, n)` - сдвиги битов

## Продвинутые возможности

### JIT-компиляция

VladX включает JIT-компилятор, который может компилировать код VladX в JavaScript для улучшения производительности.

### Продвинутая система типов

Поддерживает объединённые типы (`число|строка`), пересечённые типы, обобщённые типы, псевдонимы типов и кортежи.

### Система модулей

Улучшенная система модулей с динамическими импортами, условными экспортами, ре-экспортами и графами зависимостей.

### Инструменты разработки

- **Отладчик**: Точки останова, шаг внутрь/над/вне, отслеживание выражений, инспекция стека вызовов
- **Профилировщик**: Flame-графы, обнаружение горячих точек, профилирование памяти, сравнение производительности
- **REPL**: Интерактивная консоль с историей, автодополнением, многострочным вводом
- **Linter**: Линтинг кода с правилами стиля, автоисправлением, экспортом JUnit
- **Formatter**: Форматирование кода с настраиваемым стилем
- **Test Runner**: Фреймворк для модульного тестирования с describe/it, хуками, утверждениями, snapshot-тестированием

### Сборка и оптимизация

- **Bundler**: Сборка модулей с форматами ESM/CJS/IIFE/UMD, картами источников, tree shaking, минификацией
- **Minifier**: Минификация кода с удалением комментариев, обфускацией имён, удалением мертвого кода
- **Transformer**: AST-трансформации включая поднятие, свёртку констант, инлайнинг функций, удаление мертвого кода

### Управление средой

Улучшенная среда с кэшированием, безопасностью, плагинами, middleware и хуками.

## Поддержка

- 🐛 [Баг-трекер](https://github.com/murfivladi/vladx-main/issues) — сообщайте о проблемах
- 💬 [Обсуждения](https://github.com/murfivladi/vladx-main/discussions) — задавайте вопросы
- 📚 [Документация](https://github.com/murfivladi/vladx-main/wiki) — полная справка

## Лицензия

MIT License — см. файл [LICENSE](LICENSE) для деталей

## Авторы

VladX Team — команда энтузиастов, создающая инструменты для русскоязычных разработчиков.

---

**Создано с ❤️ для русскоязычного сообщества**