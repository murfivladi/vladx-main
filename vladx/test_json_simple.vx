# Тест функции код/разкод с простыми значениями
печать("Тестирование функций код/разкод с простыми значениями")

# Тест с простой строкой
пусть простаяСтрока = "Привет, мир!"
пусть строкаJson = код(простаяСтрока)
печать("Строка в JSON: " + строкаJson)

пусть обратноСтрока = разкод(строкаJson)
печать("Обратно строка: " + обратноСтрока)

# Тест с числом
пусть число = 42
пусть числоJson = код(число)
печать("Число в JSON: " + числоJson)

пусть обратноЧисло = разкод(числоJson)
печать("Обратно число: " + обратноЧисло)

# Тест с массивом
пусть массив = [1, 2, "текст", истина]
пусть массивJson = код(массив)
печать("Массив в JSON: " + массивJson)

пусть обратноМассив = разкод(массивJson)
печать("Обратно массив: " + обратноМассив)

# Тест с логическим значением
пусть логическое = истина
пусть логическоеЖсон = код(логическое)
печать("Логическое в JSON: " + логическоеЖсон)

пусть обратноЛогическое = разкод(логическоеЖсон)
печать("Обратно логическое: " + обратноЛогическое)

печать("Тест простых значений завершен!")